import React from 'react'
import CSSModules from 'react-css-modules'

import classes from './PageNotFound.scss'

export const PageNotFound = () => (
  <div>
    not found
  </div>
)

export default CSSModules(PageNotFound, classes)
